# Workspace Agent Rules

- **No Em Dashes**: NEVER use em dashes (`—` or `&mdash;`) anywhere in content, metadata, or UI copy across the application. Use standard hyphens (`-`), colons (`:`), commas, or period separators instead.
